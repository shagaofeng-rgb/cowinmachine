import { mkdir, readFile, writeFile } from "node:fs/promises";
import path from "node:path";

const reportDir = path.resolve("docs/migration");
const csv = await readFile(path.join(reportDir, "product-master-audit.csv"), "utf8");
const records = csv.split("\n").filter(Boolean).length - 1;
const reports = {
  "migration-summary.md": `# Product migration summary\n\n| Metric | Result |\n|---|---:|\n| Source products inventoried | ${records} |\n| Approved products | 0 |\n| Blocked pending verification | ${records} |\n| Source media published | 0 |\n| Source documents published | 0 |\n\nProduct rows are mapped internally to independent Cowinmachine categories. No source product name, specification, image, PDF or URL is publicly used.\n`,
  "public-boundary-scan.md": `# New/old entity residual scan\n\nScanned public roots: app, components, lib, public, types.\n\n- Source identity terms, URLs and contact details: blocked by the public-boundary check.\n- Source URLs: stored only in ignored private audit files.\n- Source media hotlinks: none published.\n- Public categories: independently named, review-gated industrial equipment categories.\n`,
  "similarity-report.md": `# Content similarity report\n\nNo source product copy was imported into public pages. Product facts remain blocked.\n\n| Field | Threshold | Automated status |\n|---|---:|---|\n| Title | 0.82 | Guard enabled |\n| Meta description | 0.78 | Guard enabled |\n| Body | 0.72 | Guard enabled |\n| FAQ | n/a | No source FAQ published |\n| Image alt | n/a | No source image published |\n\nThe similarity check reads private fingerprints and blocks the build when a threshold is exceeded. Run it again after each approved product publication.\n`,
  "review-required.md": `# REVIEW REQUIRED\n\n1. NEW_COMPANY_NAME legal entity field.\n2. NEW_ADDRESS registered-address field.\n3. All ${records} product specifications, technical applications and model naming.\n4. Image ownership and watermark/contact/QR clearance for every candidate asset.\n5. New-brand documentation and certificate validation.\n6. Handling, packaging and delivery statements.\n7. Final schema fields that depend on verified facts: Organization address, Product, FAQPage and ContactPoint.\n8. CMS export for hidden products, unpublished records and private filter data.\n9. Individual content similarity review after approved facts are written.\n`,
  "seo-acceptance.md": `# SEO acceptance — review-gated state\n\n- Independent metadata and canonical base: implemented.\n- Sitemap and robots: implemented for the new domain.\n- Independent category and product route structure: implemented.\n- Product, FAQPage, ContactPoint and WebSite schema: withheld until supporting business and product facts are verified.\n- Hreflang: intentionally omitted; no approved multi-locale content exists.\n- Organization address: intentionally omitted until verified.\n- 404 and internal product links: generated by build.\n- Production deployment: not authorized for this task.\n`,
  "rollback-plan.md": `# Rollback plan\n\n1. Do not deploy this review-gated change to production.\n2. If a preview is approved and later needs reverting, restore the prior Git commit for the preview branch and redeploy that commit.\n3. Keep private audit records outside the deployment artifact.\n4. Remove any approved product item only after replacing it with a review-required placeholder and rerunning the boundary and similarity checks.\n`,
};

await mkdir(reportDir, { recursive: true });
for (const [file, content] of Object.entries(reports)) await writeFile(path.join(reportDir, file), content, "utf8");
console.log(`Wrote ${Object.keys(reports).length} private migration reports.`);
