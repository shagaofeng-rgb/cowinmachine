(() => {
  const fixStyles = document.createElement("link");
  fixStyles.rel = "stylesheet";
  fixStyles.href = "/assets/fixes.css";
  document.head.appendChild(fixStyles);
  const config = window.BZMAGNET_CONFIG || { brand: "BZMAGNET", contact: {} };
  const path = location.pathname.replace(/\/$/, "") || "/";
  const productLinks = [
    ["Conveyor Magnetic Separators", "/products/conveyor-magnetic-separators/", "Protection and recovery around conveyor lines"],
    ["Mineral & Bulk Separation", "/products/mineral-bulk-separation/", "Separation options for mineral and bulk material flows"],
    ["Recycling & Metal Sorting", "/products/recycling-metal-sorting/", "Equipment categories for metal recovery and sorting"],
    ["Process Magnets & Filters", "/products/process-magnets-filters/", "Magnetic filtration for process lines"]
  ];
  const industryLinks = [["Mining & Minerals","/industry-solutions/mining-minerals/"],["Cement & Aggregates","/industry-solutions/cement-aggregates/"],["Recycling","/industry-solutions/recycling/"],["Coal & Bulk Handling","/industry-solutions/coal-bulk-handling/"]];
  const newsLinks = [["News","/news/"],["Blog","/blog/"]];
  const linkList = items => items.map(([label, href]) => `<a href="${href}">${label}</a>`).join("");
  const megaList = productLinks.map(([label,href,desc]) => `<a href="${href}">${label}<span>${desc}</span></a>`).join("");
  const header = `<a class="skip-link" href="#main">Skip to content</a><header class="site-header"><div class="nav-shell"><a class="logo" href="/" aria-label="BZMAGNET home"><span class="logo-mark" aria-hidden="true"></span>${config.brand}</a><nav class="desktop-nav" aria-label="Primary navigation"><a class="nav-link" href="/">Home</a><div class="nav-item"><button class="nav-trigger" aria-expanded="false">Products</button><div class="mega"><p class="mega-title">Product categories</p><div class="mega-grid">${megaList}</div></div></div><div class="nav-item"><button class="nav-trigger" aria-expanded="false">Industry Solutions</button><div class="dropdown">${linkList(industryLinks)}</div></div><div class="nav-item"><button class="nav-trigger" aria-expanded="false">News</button><div class="dropdown">${linkList(newsLinks)}</div></div><a class="nav-link" href="/about-contact/">About & Contact</a></nav><select class="lang-select" aria-label="Language"><option value="en">EN</option><option value="ar">AR</option></select><a class="quote-button" href="/request-a-quote/">Request a Quote</a><button class="menu-button" aria-expanded="false" aria-controls="mobile-menu">Menu</button></div></header><div class="mobile-drawer" id="mobile-menu" aria-hidden="true"><div class="drawer-head"><a class="logo" href="/"><span class="logo-mark"></span>${config.brand}</a><button class="drawer-close" aria-label="Close menu">Close</button></div><a class="quote-button" href="/request-a-quote/">Request a Quote</a><nav class="mobile-nav" aria-label="Mobile navigation"><a href="/">Home</a><button class="accordion-button" aria-expanded="false">Products</button><div class="accordion-panel">${linkList(productLinks)}</div><button class="accordion-button" aria-expanded="false">Industry Solutions</button><div class="accordion-panel">${linkList(industryLinks)}</div><button class="accordion-button" aria-expanded="false">News</button><div class="accordion-panel">${linkList(newsLinks)}</div><a href="/about-contact/">About & Contact</a><div class="mobile-lang"><label for="mobile-language">Language</label><select id="mobile-language" class="lang-select"><option value="en">English</option><option value="ar">العربية</option></select></div></nav></div>`;
  const footer = `<footer class="site-footer"><div class="footer-inner"><strong><span class="logo-mark"></span>${config.brand}</strong><p>Magnetic separation equipment supply and export coordination.</p></div></footer>`;
  document.querySelector("#site-header").innerHTML = header;
  document.querySelector("#site-footer").innerHTML = footer;
  const desktopItems = [...document.querySelectorAll(".desktop-nav .nav-item")];
  const closeDesktop = (except) => desktopItems.forEach(item => { if(item !== except){ item.classList.remove("open"); item.querySelector("button").setAttribute("aria-expanded","false"); }});
  desktopItems.forEach(item => { const button=item.querySelector("button"),panel=item.querySelector(".dropdown,.mega"); button.addEventListener("click",()=>{const opening=!item.classList.contains("open");closeDesktop(item);item.classList.toggle("open",opening);panel.style.visibility=opening?"visible":"";panel.style.opacity=opening?"1":"";panel.style.transform=opening?"none":"";button.setAttribute("aria-expanded",String(opening));}); button.addEventListener("keydown",e=>{if(e.key==="Enter"){e.preventDefault();button.click();}}); });
  document.addEventListener("click", e => { if(!e.target.closest(".desktop-nav")) closeDesktop(); });
  const drawer=document.querySelector(".mobile-drawer"), menuButton=document.querySelector(".menu-button"), closeButton=document.querySelector(".drawer-close");
  const openDrawer=()=>{drawer.classList.add("open");drawer.style.visibility="visible";drawer.style.opacity="1";drawer.setAttribute("aria-hidden","false");menuButton.setAttribute("aria-expanded","true");document.body.style.overflow="hidden";closeButton.focus();};
  const closeDrawer=()=>{drawer.classList.remove("open");drawer.style.visibility="";drawer.style.opacity="";drawer.setAttribute("aria-hidden","true");menuButton.setAttribute("aria-expanded","false");document.body.style.overflow="";menuButton.focus();};
  menuButton.addEventListener("click",openDrawer);closeButton.addEventListener("click",closeDrawer);
  drawer.querySelectorAll(".accordion-button").forEach(button=>button.addEventListener("click",()=>{const panel=button.nextElementSibling,open=button.getAttribute("aria-expanded")==="true";button.setAttribute("aria-expanded",String(!open));panel.classList.toggle("open",!open);}));
  document.addEventListener("keydown",e=>{if(e.key==="Escape"){if(drawer.classList.contains("open"))closeDrawer();else closeDesktop();}if(e.key==="Tab"&&drawer.classList.contains("open")){const f=[...drawer.querySelectorAll("a,button,select")];const first=f[0],last=f[f.length-1];if(e.shiftKey&&document.activeElement===first){e.preventDefault();last.focus();}else if(!e.shiftKey&&document.activeElement===last){e.preventDefault();first.focus();}}});
  const applyLanguage = lang => { const rtl=lang==="ar"; document.documentElement.lang=lang; document.documentElement.dir=rtl?"rtl":"ltr"; localStorage.setItem("bz-language",lang); document.querySelectorAll(".lang-select").forEach(s=>s.value=lang); };
  document.querySelectorAll(".lang-select").forEach(s=>s.addEventListener("change",()=>applyLanguage(s.value)));applyLanguage(localStorage.getItem("bz-language")||"en");
  document.querySelectorAll(`a[href="${path}/"],a[href="${path}"]`).forEach(a=>a.setAttribute("aria-current","page"));
  const form=document.querySelector("#quote-form");
  if(form){const fields=[...form.querySelectorAll("[required]")];const validate=field=>{const error=document.querySelector(`#${field.id}-error`);let message="";if(!field.value.trim())message="This field is required.";else if(field.type==="email"&&!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(field.value))message="Enter a valid email address.";error.textContent=message;field.setAttribute("aria-invalid",String(Boolean(message)));return !message;};fields.forEach(f=>f.addEventListener("blur",()=>validate(f)));form.addEventListener("submit",e=>{e.preventDefault();const valid=fields.map(validate).every(Boolean);const status=document.querySelector("#form-status");if(!valid){status.textContent="Please correct the highlighted fields.";form.querySelector('[aria-invalid="true"]').focus();return;}const data=Object.fromEntries(new FormData(form));const summary=Object.entries(data).map(([k,v])=>`${k}: ${v}`).join("\n");navigator.clipboard?.writeText(summary);status.textContent=config.contact.email?`Your enquiry is ready. Please send it to ${config.contact.email}. A copy has been placed on your clipboard.`:"Your validated enquiry is ready and has been copied to your clipboard. BZMAGNET contact channels are not yet configured, so it has not been sent.";});}
})();
