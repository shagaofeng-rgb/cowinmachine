import type { Metadata } from "next";
import { PageHero } from "@/components/PageHero";
export const metadata: Metadata = { title: "Resources", description: "Replaceable technical resource placeholders for industrial equipment selection." };
export default function ResourcesPage() { const titles = ["Air Compressor Selection Guide", "Drill Rig Buying Guide", "Solar Light Tower vs Diesel Light Tower", "Magnetic Separator Selection", "Equipment Maintenance Planning", "Construction Site Safety", "Power Source Considerations", "Preparing an Equipment Inquiry"]; return <><PageHero eyebrow="Resources" title="Technical Resources" description="General structural placeholders. Replace each item with verified technical content before publication." /><section className="section section-alt"><div className="content-wrap grid">{titles.map((title) => <article className="card" key={title}><h2>{title}</h2><p>TODO: Replace with verified product specifications.</p></article>)}</div></section></>; }
