import type { Metadata } from "next";
import Link from "next/link";
import { PageHero } from "@/components/PageHero";
import { productCategories, products } from "@/lib/products";
export const metadata: Metadata = { title: "Products & Equipment", description: "Browse replaceable equipment placeholders across compressed air, drilling, mobile lighting and material separation." };
export default function ProductsPage() { return <><PageHero eyebrow="Products" title="Products & Equipment" description="Four equipment categories for mining, construction, remote power and material processing applications." /><section className="section"><div className="content-wrap"><div className="grid">{productCategories.map((category) => <article className="card" key={category.slug}><div className="placeholder-image">TODO: Replace with authorized product image.</div><h2>{category.name}</h2><p>{category.summary}</p><Link className="button button-outline" href={`/products/${category.slug}`}>Explore Category</Link></article>)}</div></div></section><section className="section section-alt"><div className="content-wrap"><h2>Featured Equipment</h2><div className="grid">{products.slice(0, 4).map((product) => <article className="card" key={product.id}><h3>{product.name}</h3><p>{product.shortDescription}</p><Link className="button button-outline" href={`/products/${product.category}/${product.slug}`}>View Details</Link></article>)}</div></div></section></>; }
