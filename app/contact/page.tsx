import type { Metadata } from "next";
import { InquiryForm } from "@/components/forms/InquiryForm";
import { PageHero } from "@/components/PageHero";
import { siteConfig, whatsappHref } from "@/lib/site";
export const metadata: Metadata = { title: "Contact", description: "Contact COWIN MACHINE to discuss your industrial equipment application and requirements." };
export default function ContactPage() { return <><PageHero eyebrow="Contact" title="Contact Our Team" description="Share your application and equipment requirements to begin a focused discussion." /><section className="section"><div className="content-wrap quote-layout"><div><h2>Contact details</h2><p><strong>Email:</strong> <a href={`mailto:${siteConfig.email}`}>{siteConfig.email}</a></p><p><strong>WhatsApp:</strong> <a href={whatsappHref}>{siteConfig.phone}</a></p><p><strong>Address:</strong><br />{siteConfig.address.line1}<br />{siteConfig.address.line2}<br />{siteConfig.address.city}, {siteConfig.address.region}, {siteConfig.address.country}</p><p><strong>Business hours:</strong> TODO: Replace with verified COWIN MACHINE company information.</p><div className="placeholder-image">Map placeholder — replace with an authorized map configuration.</div></div><div><h2>Send an inquiry</h2><InquiryForm /></div></div></section></>; }
