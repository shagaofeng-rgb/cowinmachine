import type { Metadata } from "next";
import { PageHero } from "@/components/PageHero";
export const metadata: Metadata = { title: "Factory & Quality", description: "Replaceable information area for verified manufacturing, integration and quality documentation." };
export default function FactoryQualityPage() { return <><PageHero eyebrow="Quality" title="Factory & Quality Control" description="TODO: Replace with verified COWIN MACHINE company information." /><section className="section"><div className="content-wrap grid">{["Factory exterior", "Workshop", "Assembly", "Testing", "Warehouse", "Export packing"].map((item) => <div className="placeholder-image" key={item}>{item}: TODO: Replace with authorized product image.</div>)}</div></section><section className="section section-alt"><div className="content-wrap"><h2>Quality Process</h2><p>Incoming Inspection → Assembly → Performance Testing → Final Inspection → Packing</p><p>TODO: Replace with verified certificate or test document.</p></div></section></>; }
